package com.runloyal.booking.web; import com.runloyal.booking.domain.Model; import jakarta.validation.constraints.*; import java.math.*; import java.time.*; import java.util.*;
public final class Dto { private Dto(){} public record ServiceCommand(@NotBlank String name,String description,String category,@Positive int durationMinutes,@DecimalMin("0.0") BigDecimal price, Model.Status status){} public record StaffCommand(@NotBlank String name,Model.Status status){} public record AvailabilityCommand(@NotNull DayOfWeek dayOfWeek,@NotNull LocalTime startTime,@NotNull LocalTime endTime,@NotNull Model.AvailabilityType type){} public record BookingCommand(@NotNull UUID serviceId,@NotNull UUID staffId,@NotNull Instant startAt,@NotBlank String customerName,@NotBlank String petName){} public record ApiError(Instant timestamp,int status,String code,String message,String path,Map<String,String> validationErrors){} }
