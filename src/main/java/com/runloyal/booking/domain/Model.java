package com.runloyal.booking.domain;
public final class Model { private Model(){} public enum Status {ACTIVE, INACTIVE} public enum Role {TENANT_ADMIN, STAFF} public enum AvailabilityType {WORKING, BREAK, OFF} public enum BookingStatus {CONFIRMED, CANCELLED} }
